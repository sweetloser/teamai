# 详细工作流（Module A/B/C）

本文件包含各 Module 的详细执行步骤、命令示例和 draft.md 写入模板。

---

## Module A — 需求与资料获取（主 Agent 执行）

核心原则：钉钉文档优先获取内部资料，外部检索补充技术选型与最佳实践。每个步骤产出的关键信息必须写入 `draft.md`，作为 Writer Agent 的输入素材。

### A1. 需求理解

**目标**：完整理解业务目标、功能边界、非功能要求与约束条件。

**钉钉文档检索**：
```bash
# 搜索 PRD / 需求文档
dws doc search --keyword "{项目名称} PRD 需求文档" --format json

# 搜索产品设计文档
dws doc search --keyword "{项目名称} 产品设计 功能规格" --format json
```

**读取文档内容**（从搜索结果中提取 nodeId）：
```bash
# 读取找到的需求文档
dws doc read --node <DOC_ID> --format json
```

**写入 draft.md**（使用本地文件操作）：
```markdown
## 需求理解

### 业务背景
- 业务痛点：当前系统在高峰期响应缓慢，用户体验差，且维护成本持续上升
- 项目目标：重构核心链路，提升系统性能和可维护性，降低运营成本
- 关键干系人：产品负责人、技术负责人、运维团队、业务运营

### 功能需求
| 需求编号 | 需求描述 | 优先级 | 来源 |
|---------|---------|--------|------|
| REQ-001 | 用户注册登录流程优化 | P0 | PRD v1.5 |
| REQ-002 | 订单查询性能优化 | P0 | 用户投诉反馈 |
| REQ-003 | 数据导出功能 | P1 | 业务部门需求 |
| REQ-004 | 移动端适配 | P2 | 产品规划 |

### 非功能需求
- 性能：接口响应 P99 < 150ms，峰值 QPS >= 8000
- 可用性：SLA >= 99.9%，月度故障时间 < 43 分钟
- 安全：敏感字段 AES-128 加密，接口签名验证
- 扩展性：支持未来 2 年 5 倍业务量增长，支持水平扩展

### 约束条件
- 技术栈约束：基于 Go 语言开发，使用公司标准中间件
- 时间约束：需在 Q3 上线，开发周期 3 个月
- 人力约束：后端 4 人，前端 3 人，测试 2 人
- 预算约束：云资源年度预算控制在 50 万以内
```

---

### A2. 现有系统调研

**目标**：了解当前系统架构、技术债、可复用组件，避免重复建设。

**钉钉文档检索**：
```bash
# 搜索技术架构文档
dws doc search --keyword "{项目名称} 技术架构 系统设计" --format json

# 搜索架构图、部署图
dws doc search --keyword "{项目名称} 架构图 部署图" --format json

# 搜索相关系统接口文档
dws doc search --keyword "{相关系统} 接口文档 API" --format json
```

**读取文档内容**：
```bash
dws doc read --node <DOC_ID> --format json
```

**写入 draft.md**：
```markdown
## 现有系统调研

### 当前架构概述
- 架构模式：三层单体架构（表现层、业务层、数据层）
- 核心技术栈：Java 11、Spring Boot、MySQL 5.7、Redis 6.0
- 部署方式：传统虚拟机部署，使用 Nginx 负载均衡

### 可复用组件
| 组件 | 能力 | 负责团队 | 是否可直接复用 |
|------|------|---------|--------------|
| 认证网关 | 统一认证、路由转发 | 基础架构组 | 是 |
| 配置中心 | 动态配置、灰度发布 | 平台团队 | 是 |
| 日志平台 | 日志采集、检索分析 | 运维团队 | 是 |
| 监控告警 | 指标采集、异常告警 | SRE 团队 | 是 |
| 文件存储 | 对象存储、CDN 加速 | 存储团队 | 是 |

### 已知问题与技术债
- 代码耦合严重：业务逻辑与数据库访问层未分离，单元测试覆盖率低
- 缺少熔断机制：第三方服务故障会导致级联失败
- 数据一致性：分布式事务采用最终一致性，缺少补偿机制

### 现有接口依赖
- 上游：支付中心（Dubbo）、会员服务（RESTful API）
- 下游：库存服务（RocketMQ）、物流服务（gRPC）、风控服务（HTTP）
```

---

### A3. 技术调研

**目标**：调研行业最佳实践、架构模式和候选技术方案，为选型决策提供依据。

**web_search 查询**（多角度）：
```
- "{技术场景} 架构设计 最佳实践 2024 2025"
- "{技术A} vs {技术B} 对比 选型"
- "{业务场景} 技术方案 大厂实践"
- "{技术场景} system design architecture"
- "{中间件} 生产环境 踩坑 经验"
```

**web_fetch 深度阅读**：
```
读取技术博客/官方文档，提取架构设计、技术选型理由、性能数据、踩坑经验
```

**典型调研方向**：
- 架构模式：微服务 vs 单体、事件驱动 vs 请求驱动、CQRS、Serverless
- 数据存储：MySQL vs PostgreSQL vs TiDB、Redis vs Memcached、ES vs Meilisearch
- 消息队列：Kafka vs RocketMQ vs Pulsar
- 服务通信：gRPC vs HTTP/REST vs GraphQL
- 容器编排：Kubernetes 配置、Service Mesh

**写入 draft.md**：
```markdown
## 技术调研

### 架构模式调研
- 方案一（分层微服务）：按业务域拆分，独立部署扩展，适合复杂业务场景
- 方案二（六边形架构）：核心业务逻辑与外部依赖隔离，便于测试和维护
- 方案三（CQRS + 事件溯源）：读写分离，适合高并发写入场景

### 关键技术对比
| 维度 | 技术A | 技术B | 技术C |
|------|-------|-------|-------|
| 性能表现 | 优秀 | 良好 | 中等 |
| 学习曲线 | 平缓 | 陡峭 | 适中 |
| 社区支持 | 活跃 | 一般 | 稳定 |
| 运维复杂度 | 低 | 高 | 中 |
| 成本投入 | 低 | 高 | 中 |

### 行业参考案例
- Netflix：采用微服务架构支撑全球流媒体服务，日均处理数十亿请求（来源：Netflix Tech Blog，2024）
- Shopify：从单体迁移到微服务，部署时间从 30 分钟缩短到 2 分钟（来源：Engineering Blog，2024）
- Uber：使用事件驱动架构处理实时订单，延迟降低至毫秒级（来源：Uber Engineering，2024）
```

---

### A4. 性能与规模基准

**目标**：获取候选技术组件的性能基准数据，为容量规划和性能目标设定提供依据。

**web_search 查询**：
```
- "{技术组件} benchmark 性能测试 QPS 延迟"
- "{技术组件} 大规模 生产环境 性能优化"
- "{业务场景} 容量规划 capacity planning"
- "{数据库} 单表 亿级 性能 分库分表"
- "{缓存} 集群 内存优化 淘汰策略"
```

**web_fetch 深度阅读**（官方性能报告、第三方评测）：
```
读取性能报告，提取 QPS、延迟分布（P50/P99/P999）、资源消耗、测试条件
```

**写入 draft.md**：
```markdown
## 性能与规模基准

### 基准测试数据
| 组件 | 场景 | QPS | P99 延迟 | 资源配置 | 来源 |
|------|------|-----|---------|---------|------|
| PostgreSQL 16 | OLTP 事务处理 | 12,000 | 10ms | 8C32G NVMe | pgbench, 官方文档 |
| Dragonfly | 缓存读写 | 150,000+ | 0.5ms | 4C16G | 官方 benchmark |
| RabbitMQ 3.12 | 消息吞吐 | 85,000 msg/s | 3ms | 3 节点 4C8G | PerfTest 工具 |
| Elasticsearch 8.11 | 全文搜索 | 5,000 queries/s | 50ms | 3 节点 8C16G | Rally 基准测试 |

### 容量估算参考
- 日活用户：50 万 -> 峰值 QPS 估算：8000（按 15% 集中在 1 小时算）
- 数据增长：日增 20 万条 -> 年增 80 GB -> 3 年 240 GB
- 存储选型：单表超过 3000 万行需分表，超过 300GB 需分库

### 性能风险点
- 连接池耗尽：高峰期数据库连接数可能达到上限
- 缓存击穿：热点 Key 过期瞬间大量请求穿透到数据库
- 网络带宽：跨机房数据同步可能成为瓶颈
```

---

### A5. 安全与合规

**目标**：确认安全要求和合规约束，确保技术方案满足安全基线。

**web_search 查询**：
```
- "{业务场景} 安全 最佳实践 OWASP"
- "{技术栈} 安全加固 配置"
- "{行业} 合规要求 等保 数据安全"
- "API 安全 鉴权 限流 防护"
- "{数据类型} 加密 脱敏 隐私保护"
```

**钉钉文档检索**（内部安全规范）：
```bash
# 搜索公司安全规范
dws doc search --keyword "安全规范 安全基线" --format json

# 搜索数据安全处理规范
dws doc search --keyword "数据分类分级 敏感数据处理" --format json
```

**写入 draft.md**：
```markdown
## 安全与合规

### 安全要求
- 认证鉴权：OIDC + JWT，Token 有效期 1 小时，支持刷新令牌
- 数据加密：传输层 TLS 1.2，存储层 AES-128 加密敏感字段，密钥轮换周期 90 天
- 接口防护：限流（滑动窗口算法）、防重放（请求签名）、XSS/CSRF 防护
- 审计日志：所有敏感操作记录操作人、时间、IP、变更内容，保留 180 天

### 合规要求
- 等保要求：二级等保（如适用）
- 数据合规：遵循个人信息保护法，用户可随时撤销授权
- 行业规范：符合行业数据安全标准，定期进行安全审计

### 安全架构参考
- 网关层：API Gateway + WAF，统一认证和流量清洗
- 服务层：服务间双向认证，基于 RBAC 的权限控制
- 数据层：敏感数据列级加密，数据库审计日志，备份加密存储
```

---

## Module B — 结构化分析与整理（主 Agent 执行）

核心原则：基于 Module A 收集的素材，进行系统性分析，形成可落地的技术决策。分析结果写入 `draft.md`，作为方案正文的核心内容。

### B1. 需求拆解与优先级

将收集到的需求按功能域拆解，明确优先级和依赖关系：

```markdown
## 需求拆解

### 功能域划分
| 功能域 | 核心需求 | 优先级 | 依赖关系 | 预估工作量 |
|--------|---------|--------|---------|-----------|
| 内容管理 | 内容发布/编辑/审核 | P0 | 无 | 12 人日 |
| 搜索推荐 | 全文搜索/智能推荐 | P0 | 内容管理 | 20 人日 |
| 用户中心 | 账号管理/权限控制 | P1 | 无 | 8 人日 |
| 通知中心 | 消息推送/订阅管理 | P2 | 用户中心 | 6 人日 |

### 需求优先级矩阵
- P0（必须）：核心业务功能，直接影响用户价值，首期必须交付
- P1（重要）：增强型功能，提升产品竞争力，第二期交付
- P2（期望）：辅助型功能，完善用户体验，后续迭代优化
```

---

### B2. 技术选型评估矩阵

对每个关键技术决策点，建立多维度评估矩阵。

> **覆盖要求**：不能只对最核心的一个组件做选型分析。每个基础设施组件（存储、缓存、消息队列、服务框架、搜索引擎等）都须有独立的选型分析。如果某个组件选型理由非常明确（如"团队已有 3 年生产经验"），可以简化为一段文字说明，但不能完全省略。

```markdown
## 技术选型评估

### 消息队列选型
| 评估维度 | RabbitMQ | NATS | RedPanda | 权重 |
|---------|----------|------|----------|------|
| 吞吐性能 | 85,000 msg/s | 120,000 msg/s | 200,000 msg/s | 30% |
| 延迟表现 | 3ms P99 | 1ms P99 | 2ms P99 | 25% |
| 运维复杂度 | 中 | 低 | 中 | 20% |
| 消息可靠性 | 高（持久化） | 中（内存为主） | 高（持久化） | 15% |
| 学习曲线 | 平缓 | 陡峭 | 平缓 | 10% |
| **综合得分** | **82** | **76** | **71** | — |

**结论**：选择 RabbitMQ，理由：成熟稳定，消息可靠性高，团队有使用经验。吞吐性能满足当前需求（峰值 5 万 msg/s），运维成本可控。未来如需更高吞吐，可平滑迁移到 RedPanda。
```

---

### B3. 架构设计原则

> **整体方案对比**：在展开详细架构设计前，须先列出 2-3 个整体方案进行对比。例如"方案 A: 微服务架构" vs "方案 B: 模块化单体" vs "方案 C: Serverless"，从性能、成本、团队能力、演进路径等维度对比优劣，明确选择理由后再展开详细设计。

明确本方案遵循的设计原则，作为后续模块设计的指导：

```markdown
## 架构设计原则

1. **职责单一**：每个模块只负责一个业务领域，边界清晰，职责明确
2. **接口稳定**：服务间通过稳定的 API 契约通信，版本化管理，向后兼容
3. **弹性设计**：系统具备容错能力，依赖服务故障时可自动降级或熔断
4. **可验证性**：关键决策基于数据和指标，通过 A/B 测试验证假设
5. **安全优先**：安全机制贯穿设计、开发、运维全生命周期，而非事后补救
6. **全链路可见**：从请求入口到数据存储，每个环节都有日志和监控覆盖
```

---

### B4. 关键模块设计

对核心模块进行详细设计，包括职责、接口、数据模型和关键逻辑：

```markdown
## 关键模块设计

### 模块划分
- 根据功能域划分和依赖关系，确定模块边界
- 每个模块明确：职责范围、对外接口、数据模型、关键算法/逻辑

### 设计要素（每个模块）
1. **职责**：该模块负责什么，不负责什么（明确边界）
2. **接口设计**：入参、出参、错误码、幂等性
3. **数据模型**：核心表结构、索引设计、数据生命周期
4. **关键逻辑**：核心算法、状态机、并发控制策略
5. **依赖关系**：上下游依赖、降级策略
```

---

### B5. 性能与容量估算

基于需求和基准数据，进行系统性的容量估算：

```markdown
## 性能与容量估算

### 流量模型
- 日活用户（DAU）：30 万
- 人均请求数：25 次/天
- 日请求总量：750 万次
- 峰值系数：4 倍（业务特征分析）
- 峰值 QPS：3500

### 资源估算
| 服务 | 实例数 | 单实例配置 | 峰值 CPU | 峰值内存 | 月成本 |
|------|--------|-----------|---------|---------|--------|
| 网关服务 | 2 | 2C4G | 55% | 45% | 1200 元 |
| 订单服务 | 4 | 4C8G | 65% | 55% | 3600 元 |
| 搜索服务 | 3 | 8C16G | 70% | 60% | 5400 元 |
| PostgreSQL | 1主1从 | 4C16G 200G SSD | 35% | 55% | 2800 元 |
| Dragonfly | 3 节点 | 2C8G | 25% | 65% | 1800 元 |

### 扩展预案
- 短期（6个月）：当前配置可支撑 3 倍流量增长
- 中期（18个月）：扩容订单服务至 8 实例，搜索服务增加副本分片
- 长期（36个月）：评估引入读写分离或分库分表方案
```

---

### B6. 风险评估与降级方案

系统性识别技术风险，制定降级预案：

```markdown
## 风险评估

### 风险清单
| 风险 | 概率 | 影响 | 降级方案 | 负责人 |
|------|------|------|---------|--------|
| 搜索服务不可用 | 中 | 严重 | 降级为数据库查询，限制返回数量 | 搜索团队 |
| 支付接口超时 | 高 | 严重 | 重试 3 次，失败后进入人工处理队列 | 支付团队 |
| 消息队列堆积 | 低 | 中 | 扩容消费者，启用批量消费 | 后端团队 |
| 数据库连接耗尽 | 中 | 严重 | 启用连接池熔断，拒绝非核心请求 | DBA |
| CDN 故障 | 低 | 中 | 切换至备用 CDN 源站 | 运维团队 |

### 降级层级
- 一级（自动响应）：系统自动触发降级策略，无需人工干预
- 二级（配置切换）：通过配置平台下发降级配置，秒级生效
- 三级（人工处理）：需要技术团队介入排查和恢复
```

---

## Module C — 调用 Writer Agent 生成钉钉文档

### 任务分发

主 Agent 完成数据收集和结构化分析后，调用 `task` 工具分发给 Writer Agent：

```json
{
  "description": "生成技术方案钉钉文档",
  "prompt": "基于 /home/workspace/draft.md 中的技术方案素材，生成一份结构完整的钉钉文档技术方案。\n\n要求：\n1. 严格按照技术方案文档模板结构\n2. 方案摘要（TL;DR）放在最前面，3-5 条核心设计决策\n3. 技术选型必须有评估矩阵和明确结论\n4. 性能数据标注测试条件和来源\n5. 风险清单完整，每条风险有降级方案\n6. 接口定义使用代码块\n\n文档模板见下方。",
  "subagent_type": "writer"
}
```

### Writer Agent 执行流程

```
Writer Agent 接收任务
      |
      v
1. 读取 draft.md 素材文件
      |
      v
2. 基于素材和模板生成结构化大纲
   - 输入：draft.md 内容 + 技术方案模板
   - 输出：带层级的大纲结构
      |
      v
3. 创建钉钉文档并写入内容
   步骤 3a: 创建文档
     dws doc create --name "{方案名称} 技术方案" --format json
     → 提取返回的 nodeId
   步骤 3b: 写入完整内容（追加模式）
     dws doc update --node <nodeId> --markdown "<完整方案内容>" --mode append --format json
   TRAP: markdown 内容不要以 # 一级标题开头！dws doc create --name 已经设置了文档标题，
         如果 markdown 内容再以 # 标题开头，会导致文档中出现两个重复标题。
         正文应直接从第一个章节开始（如 ## 一、项目背景与目标）
   TRAP: markdown 参数中的换行必须使用真实换行符（Unicode U+000A），
         不能使用字面量 \n，否则所有内容会渲染在同一行
   NOTE: 架构图建议使用 Markdown 表格或层级列表描述组件关系，避免 ASCII Art
         在钉钉文档中渲染异常（等宽字体不保证、对齐会错位）
      |
      v
4. 返回钉钉文档链接
   文档 URL 格式: https://alidocs.dingtalk.com/i/nodes/{nodeId}
```

### 钉钉文档操作要点

1. MUST: 创建文档时使用 `dws doc create --name "文档标题" --format json`
2. MUST: 写入内容时使用 `dws doc update --node <nodeId> --markdown "<内容>" --mode append --format json`
3. TRAP: markdown 内容禁止以 `# 标题` 开头，因为 `dws doc create --name` 已设置文档标题，重复会导致两个标题。正文从 `##` 二级标题开始
3. SHOULD: 如果内容很长，可以分多次追加写入，每次写入一个章节
4. SHOULD: 如需指定存放位置，可通过 `--folder <FOLDER_ID>` 指定文件夹
5. TRAP: `--mode overwrite` 会清空原内容后重写，谨慎使用；默认用 `--mode append`
6. TRAP: markdown 中的换行必须是真实换行符，不能是字面量 `\n`
7. NOTE: 如需精细编辑（如插入表格到特定位置），可使用 `dws doc block insert` 系列命令

### 任务初始化

收到用户请求后，立即创建任务列表：

```json
{
  "todos": [
    {"content": "需求理解：梳理业务目标、功能需求、非功能需求与约束条件", "status": "in_progress"},
    {"content": "现有系统调研：检索钉钉文档中的技术文档、架构图、已有组件能力", "status": "pending"},
    {"content": "技术调研：检索架构设计最佳实践、技术选型对比、行业方案", "status": "pending"},
    {"content": "性能与规模基准：获取技术组件性能数据、容量规划参考", "status": "pending"},
    {"content": "安全与合规调研：检索安全最佳实践、合规要求", "status": "pending"},
    {"content": "结构化分析：需求拆解、选型评估、架构设计、风险评估", "status": "pending"},
    {"content": "调用 Writer Agent 生成技术方案钉钉文档", "status": "pending"}
  ]
}
```
